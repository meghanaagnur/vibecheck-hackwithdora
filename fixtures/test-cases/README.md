# Test cases

## `login-page` (the Hour 0–2 golden test case)

The ONE simple case the whole pipeline gets built against first:

> login page: email input, password input, blue submit button below

Add a `login-page/` folder here with:
- `prompt.txt` — the prompt text
- `code.html` (or `.jsx`) — a hand-written or once-generated example to render, so Aalok
  isn't blocked waiting on Aryan's code-gen agent during Day 1

Add more test cases here during Day 2 (hours 24–34) as Aalok/Aryan generalize past
buttons/inputs to headings, images, forms.
