// checklist.js (Aryan) — prompt -> checklist.json
// See docs/SCHEMA.md section 1 and schema/checklist.example.json for the target shape.

export async function generateChecklist(/* prompt */) {
  throw new Error("TODO: call Claude API, parse into checklist.json shape");
}
