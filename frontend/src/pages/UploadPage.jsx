// UploadPage (You + Duchuu)
// Prompt/image input + code input, plus the "generate via agent" option (dropdown with
// one enabled agent, "more agents coming soon" per the execution plan).
export default function UploadPage() {
  return (
    <div>
      <h1>VibeCheck</h1>
      <p>TODO: prompt input, code input, agent-generate option.</p>
    </div>
  );
}
