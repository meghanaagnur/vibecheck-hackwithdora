// FallbackGallery (You + Duchuu) — Day 2, hours 40-44
// One-click loader for pre-generated demo runs in /fixtures/demo-gallery.
export default function FallbackGallery({ onSelect }) {
  return (
    <div>
      {/* TODO: list fixtures/demo-gallery entries, onSelect(diffJson) */}
    </div>
  );
}
